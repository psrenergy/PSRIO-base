local generationconstraint = GenerationConstraint(); 
warning("generation_constraint = require(\"collection/generationconstraint\") will be deprecated in the future, use generation_constraint = GenerationConstraint();");
return generationconstraint;
